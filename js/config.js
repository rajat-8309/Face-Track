/* ============================================================
   config.js — Supabase Project Credentials
   ============================================================
   1. Go to https://supabase.com/dashboard
   2. Open your project → Settings → API
   3. Copy "Project URL" into SUPABASE_URL below
   4. Copy "anon / public" key into SUPABASE_ANON_KEY below
   5. Save this file — the app will connect automatically

   The anon key is safe to commit to a public GitHub repo.
   It is designed to be public. Security is handled by
   Row Level Security policies in Supabase + the app's
   own branch-password system.
   ============================================================ */

window.SUPABASE_URL      = 'https://YOUR_PROJECT_ID.supabase.co';
window.SUPABASE_ANON_KEY = 'YOUR_ANON_PUBLIC_KEY_HERE';
